# Catalog integration test branch

This branch is a disposable integration lane created from `safe-test-checkpoint`.

No production branch is modified here. The purpose is to test the browser integration wiring before any pull request to `main`.

If the integration test fails, this branch can be discarded without affecting the catalog in `main`.
