# Safe modular catalog work

This directory is a staging area for the next catalog architecture. The live `index.html` is intentionally left untouched until the modular pieces are fully tested.

Current proven fixes in `catalog-fix-test.html`:
- COA visibility follows its ON/OFF setting.
- Photo preview works and saved photo data can be restored.
- Certification remains available for certification/verification-link information.

Do not delete or replace the live catalog as part of this staging work.