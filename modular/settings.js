(function(){const defaults={coa:true,photos:true,paperMoney:true,certification:true,backup:true,ogp:true,originalBox:true,sigma:true,photosComplete:true,variety:true};window.LewisCatalogSettings={defaults,load(){try{return{...defaults,...JSON.parse(localStorage.getItem('lewis-settings')||'{}')}}catch(_){return{...defaults}}},save(s){localStorage.setItem('lewis-settings',JSON.stringify(s))},toggle(s,k){if(!(k in s))return s;const n={...s,[k]:!s[k]};this.save(n);return n},apply(s){const m={coa:'coaSection',photos:'photosSection',paperMoney:'paperMoneySection',certification:'certSection',backup:'backupSection'};Object.entries(m).forEach(([k,id])=>{const e=document.getElementById(id);if(e)e.hidden=s[k]===false})}}})();