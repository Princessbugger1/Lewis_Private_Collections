# Phone/Desktop Implementation Checkpoint

The live catalog is ready for a focused display-mode implementation.

Implementation rules:
- Add only the display controls and their local preference storage.
- Do not change the catalog record schema.
- Do not change local collection storage keys.
- Do not alter backup/restore behavior.
- Keep normal responsive CSS behavior available as the fallback.
- Make both controls easy to find on a phone.

This checkpoint precedes the live `index.html` change.